package hclust

// Tree generates a newick tree in string format and returns the ordering
// of the clustering.
func Tree(dendrogram []SubCluster, names []string) (tree string, order []string) {
	return
}
